# Python
__pycache__/
*.pyc
*.pyo
.Python
venv/

# Credentials — never commit these
.env
*.pem
*.key
.streamlit/secrets.toml

# Stock models — gitignored, regenerate by running features.py
model_*.joblib
features_*.csv
feature_importances.csv
*_meta.json

# Live trading state — runtime JSON fallbacks for Supabase tables.
# Source of truth is Supabase; these files only exist on a single machine
# and drift from cloud state. Never commit.
positions.json
paper_trades.json
lt_positions.json
conviction_positions.json
stock_real_trades.json
kalshi/positions_kalshi.json

# Claude config — user-specific
.claude/

# Kalshi trading artifacts
kalshi/debug_*.py
kalshi/monitor.log

# Generated charts
*.png

# Generated outputs — can be recreated by running the scripts
cv_results_*.csv
walk_forward_cv.png
ticker_comparison.png
